# Base URL of the deployed backend API (Replit). Must be prefixed with VITE_
# so Vite exposes it to the browser bundle. Leave empty for local dev where
# requests go to the same origin via the dev proxy.
VITE_API_URL=https://your-api.replit.app

# URL base path for the SPA. Falls back to "/" if unset.
BASE_PATH=/
